[pytest]
testpaths = tests
markers =
    integration: network or live AI — excluded from default no-quota unit runs
    ui: browser/playwright tests (optional)
filterwarnings =
    ignore::DeprecationWarning
